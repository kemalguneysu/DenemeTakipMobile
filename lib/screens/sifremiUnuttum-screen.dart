import 'package:flutter/material.dart';

class SifremiUnuttumScreen extends StatelessWidget {
  const SifremiUnuttumScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Şifremi Unuttum",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
